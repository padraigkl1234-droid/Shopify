<div class="announce">{{ section.settings.text }}</div>

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    { "type": "text", "id": "text", "label": "Message", "default": "FREE UK SHIPPING OVER £60 — NEW DROP LIVE NOW" }
  ],
  "presets": [{ "name": "Announcement bar" }]
}
{% endschema %}
